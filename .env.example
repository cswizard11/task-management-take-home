# Environment variables for Docker Compose

# This secret is used by the API to sign JWTs.
# Changing it will invalidate all existing tokens.
JWT_SECRET=a-very-secure-secret-for-docker-compose

# This port is exposed on your local machine for accessing the dashboard.
# The application will be accessible at http://localhost:${DASHBOARD_PORT}
DASHBOARD_PORT=8080
